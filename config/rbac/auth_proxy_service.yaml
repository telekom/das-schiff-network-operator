apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: network-operator
  name: -metrics
  namespace: system
spec:
  ports:
  - name: metrics
    port: 9443
    protocol: TCP
    targetPort: metrics
  selector:
    app.kubernetes.io/name: network-operator
