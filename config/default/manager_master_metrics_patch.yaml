apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: master
  namespace: system
spec:
  template:
    spec:
      containers:
      - name: manager
        ports:
        - containerPort: 7080
          name: metrics
          protocol: TCP

