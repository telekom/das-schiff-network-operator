apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: worker
  namespace: system
spec:
  template:
    spec:
      containers:
      - name: manager
        ports:
        - containerPort: 7080
          name: mertics
          protocol: TCP
