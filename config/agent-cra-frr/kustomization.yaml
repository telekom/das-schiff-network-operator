resources:
- agent.yaml
- service.yaml

generatorOptions:
  disableNameSuffixHash: true

configMapGenerator:
- files:
  - config.yaml
  - frr.conf.tpl
  name: config
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
