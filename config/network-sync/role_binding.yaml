apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: network-sync-rolebinding
  labels:
    app.kubernetes.io/component: network-sync
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: network-sync-role
subjects:
  - kind: ServiceAccount
    name: network-sync
    namespace: system
