---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: nodeconfigprocesses.network.schiff.telekom.de
spec:
  group: network.schiff.telekom.de
  names:
    kind: NodeConfigProcess
    listKind: NodeConfigProcessList
    plural: nodeconfigprocesses
    shortNames:
    - ncp
    singular: nodeconfigprocess
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.state
      name: State
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NodeConfigProcess is the Schema for the node configuration process
          state.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NodeConfigSpec defines the desired state of NodeConfig.
            properties:
              state:
                type: string
            required:
            - state
            type: object
        type: object
    served: true
    storage: true
    subresources: {}
