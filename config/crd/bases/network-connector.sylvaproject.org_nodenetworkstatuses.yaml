---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: nodenetworkstatuses.network-connector.sylvaproject.org
spec:
  group: network-connector.sylvaproject.org
  names:
    kind: NodeNetworkStatus
    listKind: NodeNetworkStatusList
    plural: nodenetworkstatuses
    shortNames:
    - nns
    singular: nodenetworkstatus
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.lastUpdated
      name: LastUpdated
      type: date
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          NodeNetworkStatus is the Schema for the nodenetworkstatuses API.
          It represents per-node network inventory populated by CRA agents.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NodeNetworkStatusSpec is intentionally empty; the resource
              is agent-populated.
            type: object
          status:
            description: NodeNetworkStatusStatus contains the observed network state
              of a node.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the resource's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              interfaces:
                description: Interfaces lists the network interfaces on the node.
                items:
                  description: NodeInterface describes a network interface on a node.
                  properties:
                    addresses:
                      description: Addresses lists assigned IP addresses.
                      items:
                        type: string
                      type: array
                    mac:
                      description: Mac is the MAC address of the interface.
                      type: string
                    members:
                      description: Members lists bonded member interfaces (for type=bond).
                      items:
                        type: string
                      type: array
                    mtu:
                      description: Mtu is the maximum transmission unit.
                      format: int32
                      type: integer
                    name:
                      description: Name is the interface name.
                      type: string
                    parent:
                      description: Parent is the parent interface (for type=vlan).
                      type: string
                    state:
                      description: State is the link state.
                      enum:
                      - up
                      - down
                      - unknown
                      type: string
                    type:
                      description: Type is the interface type.
                      enum:
                      - physical
                      - bond
                      - vlan
                      - bridge
                      - vxlan
                      - loopback
                      - virtual
                      type: string
                    vlanID:
                      description: VlanID is the VLAN ID (for type=vlan).
                      format: int32
                      type: integer
                  required:
                  - name
                  - state
                  type: object
                type: array
              lastUpdated:
                description: LastUpdated is the timestamp of the last status update.
                format: date-time
                type: string
              routes:
                description: Routes lists the routing table entries on the node.
                items:
                  description: NodeRoute describes a routing table entry on a node.
                  properties:
                    destination:
                      description: Destination is the route destination in CIDR notation.
                      type: string
                    gateway:
                      description: Gateway is the next-hop gateway address.
                      type: string
                    interface:
                      description: Interface is the egress interface for this route.
                      type: string
                    table:
                      description: Table is the routing table name.
                      type: string
                  required:
                  - destination
                  - interface
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
