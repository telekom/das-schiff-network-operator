---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: trafficmirrors.network-connector.sylvaproject.org
spec:
  group: network-connector.sylvaproject.org
  names:
    kind: TrafficMirror
    listKind: TrafficMirrorList
    plural: trafficmirrors
    shortNames:
    - tmir
    singular: trafficmirror
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.source.kind
      name: SourceKind
      type: string
    - jsonPath: .spec.source.name
      name: SourceName
      type: string
    - jsonPath: .spec.collector
      name: Collector
      type: string
    - jsonPath: .spec.direction
      name: Direction
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: networkconnector
    schema:
      openAPIV3Schema:
        description: TrafficMirror declaratively mirrors traffic from an attachment
          to a Collector.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TrafficMirrorSpec defines the desired state of TrafficMirror.
            properties:
              collector:
                description: Collector is the name of the Collector resource.
                minLength: 1
                type: string
              direction:
                description: Direction is the mirror direction.
                enum:
                - ingress
                - egress
                - both
                type: string
              source:
                description: Source identifies the attachment to mirror traffic from.
                properties:
                  kind:
                    description: Kind is the type of attachment.
                    enum:
                    - Layer2Attachment
                    - Inbound
                    - Outbound
                    type: string
                  name:
                    description: Name is the name of the attachment resource.
                    minLength: 1
                    type: string
                required:
                - kind
                - name
                type: object
              trafficMatch:
                description: TrafficMatch optionally filters which traffic to mirror.
                properties:
                  dstPort:
                    description: DstPort filters by destination port (requires protocol
                      to be tcp or udp).
                    format: int32
                    maximum: 65535
                    minimum: 1
                    type: integer
                  dstPrefix:
                    description: DstPrefix filters by destination IP prefix in CIDR
                      notation.
                    type: string
                  protocol:
                    description: Protocol filters by IP protocol.
                    enum:
                    - TCP
                    - UDP
                    - ICMP
                    type: string
                  srcPort:
                    description: SrcPort filters by source port (requires protocol
                      to be tcp or udp).
                    format: int32
                    maximum: 65535
                    minimum: 1
                    type: integer
                  srcPrefix:
                    description: SrcPrefix filters by source IP prefix in CIDR notation.
                    type: string
                type: object
                x-kubernetes-validations:
                - message: srcPort/dstPort require protocol to be TCP or UDP
                  rule: (!has(self.srcPort) && !has(self.dstPort)) || (has(self.protocol)
                    && self.protocol != 'ICMP')
            required:
            - collector
            - direction
            - source
            type: object
          status:
            description: TrafficMirrorStatus defines the observed state of TrafficMirror.
            properties:
              activeNodes:
                description: ActiveNodes is the number of nodes where the mirror is
                  active.
                format: int32
                type: integer
              conditions:
                description: Conditions represent the latest available observations
                  of the TrafficMirror's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  by the controller.
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
