# NetworkAttachmentDefinition for VLAN 590 (mirror VRF)
# Uses CNI conflist to chain macvlan with the tuning meta-plugin.
# The tuning plugin disables IPv6 DAD at CNI-chain time.
apiVersion: k8s.cni.cncf.io/v1
kind: NetworkAttachmentDefinition
metadata:
  name: macvlan-vlan590
spec:
  config: |
    {
      "cniVersion": "0.4.0",
      "name": "macvlan-vlan590",
      "plugins": [
        {
          "type": "macvlan",
          "master": "vlan.590",
          "mode": "bridge",
          "ipam": {
            "type": "static"
          }
        },
        {
          "type": "tuning",
          "sysctl": {
            "net.ipv6.conf.IFNAME.accept_dad": "0"
          }
        }
      ]
    }
